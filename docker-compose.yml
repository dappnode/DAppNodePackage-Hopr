version: "3.5"
services:
  node:
    image: "node.hopr.public.dappnode.eth:1.0.20"
    build:
      context: .
      args:
        UPSTREAM_VERSION: >-
          2.1.0-rc.3@sha256:7b412b1bad4330b4ce87195cfa9ba84d852f105ba64fe30334df325317d99aee
    ports:
      - "9091-9099:9091-9099/tcp"
      - "9091-9099:9091-9099/udp"
      - "3001"
    volumes:
      - "db:/app/hoprd-db"
    security_opt:
      - "seccomp:unconfined"
    environment:
      RUST_LOG: info
      HOPRD_NETWORK: dufour
      HOPRD_CONFIGURATION_FILE_PATH: /app/hoprd.cfg.yaml
      HOPRD_HOST: "127.0.0.1:9091"
      HOPRD_SAFE_ADDRESS: 0x
      HOPRD_MODULE_ADDRESS: 0x
      HOPRD_HEARTBEAT_THRESHOLD: 60
      HOPRD_HEARTBEAT_INTERVAL: 20
      HOPRD_HEARTBEAT_VARIANCE: 1
      HOPRD_PASSWORD: ""
      HOPRD_PROVIDER: ""
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
  admin:
    image: >-
      europe-west3-docker.pkg.dev/hoprassociation/docker-images/hopr-admin:1.1.1
    ports:
      - "3000:80"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M
volumes:
  db: {}
