version: "3.5"
services:
  node:
    image: "node.hopr.public.dappnode.eth:1.0.4"
    build:
      context: .
      args:
        UPSTREAM_VERSION: 1.91.23
    ports:
      - "9091:9091/tcp"
      - "9091:9091/udp"
      - "3001"
    volumes:
      - "db:/app/hoprd-db"
    environment:
      - DEBUG=hopr*
      - HOPRD_ENVIRONMENT=monte_rosa
      - "HOPRD_HOST=0.0.0.0:9091"
      - HOPRD_ANNOUNCE=false
      - HOPRD_ADMIN=true
      - HOPRD_ADMIN_HOST=0.0.0.0
      - HOPRD_ADMIN_PORT=3000
      - HOPRD_API=true
      - HOPRD_API_HOST=0.0.0.0
      - HOPRD_API_PORT=3001
      - HOPRD_HEALTH_CHECK=true
      - HOPRD_HEALTH_CHECK_HOST=0.0.0.0
      - HOPRD_HEALTH_CHECK_PORT=8080
      - HOPRD_PASSWORD=open-sesame-iTwnsPNg0hpagP+o6T0KOwiH9RQ0
      - HOPRD_DISABLE_API_AUTHENTICATION=true
      - HOPRD_API_TOKEN=VhKw4qZdmmk3cuNpuM
      - HOPRD_IDENTITY=/app/hoprd-db/.hopr-identity
      - HOPRD_DATA=/app/hoprd-db/data
      - HOPRD_INIT=true
      - HOPRD_ALLOW_PRIVATE_NODE_CONNECTIONS=false
      - "HOPRD_PROVIDER=https://provider-proxy.hoprnet.workers.dev/xdai_mainnet"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
volumes:
  db: {}
